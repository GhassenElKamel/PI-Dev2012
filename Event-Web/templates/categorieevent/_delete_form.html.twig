<form method="post" action="{{ path('categorieevent_delete', {'id': categorieevent.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorieevent.id) }}">
    <button class="btn btn-danger btn-rounded btn-fw">Delete</button>
</form>
