{% extends 'baseBack.html.twig' %}

{% block slider %}
{% endblock %}
{% block change %}
    <section style="margin-left: 450px">
    <link rel="stylesheet" href="{{ asset('front/Event.css') }}">
    <link rel="stylesheet" href="{{ asset('back/css/style.css') }}">
    <table class="table3 table-bordered" style="margin-top: 100px">
        <tbody>
            <tr class="table-danger">
                <th>Nom</th>
                <td>{{ categorieevent.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorieevent.description }}</td>
            </tr>
            <tr class="table-success">
                <th>DbPicture</th>
                <td><img src="https://{{ categorieevent.image }}" width="100" height="100"></td>
            </tr>
        </tbody>
    </table>
    <div class="center-div" >
        <div class="margin-top"><a href="{{ path('categorieevent_index') }}"><button class="btn btn-dark btn-rounded btn-fw alink">back to list</button></a></div>
        <div class="margin-top"><a href="{{ path('categorieevent_edit', {'id': categorieevent.id}) }}"><button class="btn btn-secondary btn-rounded btn-fw alink">edit</button></a></div>
        <div class="margin-top">{{ include('categorieevent/_delete_form.html.twig') }}</div>
    </div>
    <link rel="stylesheet" href="{{ asset('front/style.css') }}">
    </section>
{% endblock %}
